# Common Components

Reserved for shared libraries, configuration schemas, and utilities consumed by both bots and services. Populate this directory as cross-cutting concerns emerge (shared protobufs, telemetry helpers, token acquisition logic, etc.).
